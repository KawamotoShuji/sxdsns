<%= form_for([chat,message], remote: true, authenticity_token: true) do |f| %>
<%= f.hidden_field :chat_id %>
<%= f.label :avatar %>
<%= f.file_field :avatar %>
<%= f.hidden_field :avatar_cache %>
<%= f.text_field :content, placeholder: "内容"%>
<%= f.submit %>
<% end %>
