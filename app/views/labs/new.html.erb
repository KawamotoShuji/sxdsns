<% if @lab.errors.any? %>
  <div id="error_explanation">
    <h2><%= @lab.errors.count %>件のエラーがあります。</h2>
    <ul>
    <% @lab.errors.full_messages.each do |msg| %>
      <p><%= msg %></p>
    <% end %>
    </ul>
  </div>
<% end %>

<%= form_for(@lab) do |f| %>
<%= f.label :title %>
<%= f.text_field :title %>
<br>
<%= f.label :body %>
<%= f.text_field :body %>
<br>
<%= f.submit "投稿"%>
<% end %>
